using ITworks.Brom.Types;

namespace ITworks.Brom {
	/// <summary>
	/// Контекст объекта вида расчета.
	/// </summary>
    public class ПланВидовРасчетаОбъект : КонтекстОбъекта {
		internal ПланВидовРасчетаОбъект(ПланВидовРасчетаСсылка ссылка) : base(ссылка) { }
	}
}
